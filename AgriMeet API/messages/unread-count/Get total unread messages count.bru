meta {
  name: Get total unread messages count
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/messages/unread-count
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
}

example {
  name: Unread count retrieved successfully
  
  request: {
    url: {{baseUrl}}/messages/unread-count
    method: GET
    mode: none
    headers: {
      Authorization: Bearer <token>
    }
  }
  
  response: {
    status: {
      code: OK
      text: 200
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
