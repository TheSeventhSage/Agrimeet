meta {
  name: Get all conversations
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/conversations?unread_only=false&context_type=product
  body: none
  auth: bearer
}

params:query {
  unread_only: false
  context_type: product
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
}

docs {
  Retrieves all conversations for the authenticated user (buyer or seller). Shows unread count and last message preview.
}

example {
  name: Conversations retrieved successfully
  
  request: {
    url: {{baseUrl}}/conversations?unread_only=false&context_type=product
    method: GET
    mode: none
    params:query: {
      unread_only: false
      context_type: product
    }
  
    headers: {
      Authorization: Bearer <token>
    }
  }
  
  response: {
    status: {
      code: OK
      text: 200
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
