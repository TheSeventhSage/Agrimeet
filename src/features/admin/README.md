# Learnings and Erro Documentation

\*Payouts.jsx

````const handleConfirmModal = () => {
        setShowConfirm((prevState) => prevState === false ? prevState = true : prevState = false); //prevState arg. keeps track of the prvious state value
}```
````
